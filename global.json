{
  "//": "Minimum SDK, not a pin. rollForward latestFeature uses the newest .NET 10 SDK installed, which keeps the embedded self-contained runtime current on rebuild.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
